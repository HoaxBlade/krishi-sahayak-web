apiVersion: v1
kind: Service
metadata:
  name: ml-server-service
  namespace: krishi-sahayak
  labels:
    app: ml-server
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
    name: http
  selector:
    app: ml-server
